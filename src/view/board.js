export default () =>{
  const board = `
  <header>
  <section class="board">
    <div>
      
    </div>
  </section>`

  const divElem = document.createElement("div");
  divElem.innerHTML = register;
  return divElem;
}